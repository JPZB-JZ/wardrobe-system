package com.aurastyle.wardrobe.ui.theme

import androidx.compose.ui.graphics.Color

// 品牌主色
val Primary = Color(0xFFc46d9c)        // 主粉色
val PrimaryLight = Color(0xFFe8c4d9)   // 浅粉色
val PrimaryDark = Color(0xFF92400e)    // 深棕色

// 次要色
val Secondary = Color(0xFFd4a574)      // 米色
val Tertiary = Color(0xFF8b5cf6)       // 紫色

// 渐变色
val GradientStart = Color(0xFFc46d9c)  // 粉
val GradientMid = Color(0xFFd4a574)    // 米
val GradientEnd = Color(0xFF8b5cf6)    // 紫

// 背景色
val Background = Color(0xFFfaf9fb)     // 主背景
val Surface = Color(0xFFFFFFFF)       // 卡片表面
val SurfaceHigh = Color(0xFFf3f0f4)   // 高亮表面

// 文字色
val TextPrimary = Color(0xFF1a1c1d)   // 主文字
val TextSecondary = Color(0xFF6b7280) // 次要文字
val TextMuted = Color(0xFF9ca3af)     // 淡化文字

// 玻璃效果色
val GlassBackground = Color(0x80FFFFFF)  // 半透明白
val GlassBorder = Color(0x40E8C4D9)      // 半透明边框